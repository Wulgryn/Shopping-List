<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SL.UI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SL.UI"
	xmlns:pages="clr-namespace:SL.UI.Pages"
    Title="ShoppingList">

	<!--<Shell.FlyoutFooter>
		<Label Text="© 2025 MyApp"/>
	</Shell.FlyoutFooter>-->

	<!--<Shell.ItemTemplate>
		<DataTemplate>
			<Grid Padding="10,20,0,20">
				--><!-- bal oldali padding csökkentése pl --><!--
				<Label Text="{Binding Title}" VerticalOptions="Center" Margin="20,0,0,0" FontSize="20" />
			</Grid>
		</DataTemplate>
	</Shell.ItemTemplate>

	<Shell.MenuBarItems>
		<MenuBarItem Text="valami">
			
		</MenuBarItem>
	</Shell.MenuBarItems>

	<FlyoutItem Title="Kezdőlap" Icon="appicon.png">
		<ShellContent ContentTemplate="{DataTemplate local:MainPage}"/>
	</FlyoutItem>

	<FlyoutItem Title="Beállítások" Route="settings">
		<ShellContent Title="Beállítások" ContentTemplate="{DataTemplate pages:AddItem}" />
	</FlyoutItem>
	<ShellContent Title="About"
                  Icon="info.png"
                  ContentTemplate="{DataTemplate pages:AddItem}" />
	<MenuItem Text="Help"/>-->

	<ShellContent
        Title=""
		
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

</Shell>
